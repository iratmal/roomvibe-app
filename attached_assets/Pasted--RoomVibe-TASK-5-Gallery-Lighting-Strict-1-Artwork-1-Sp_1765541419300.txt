# ✅ RoomVibe — TASK 5: Gallery Lighting (Strict 1 Artwork = 1 Spotlight)

**VAŽNO:** Raditi na stabilnom snapshotu (non-black room). Ako se pojavi “crna soba”, odmah rollback na stabilni snapshot i nastaviti.

---

## 🆕 TASK 5.1 — Strict “1 Artwork = 1 Spotlight” (Final discipline)

### Problem

Trenutno spotlightovi djeluju kao generički pattern po zidu: jedan snop pogađa više radova ili osvjetljava prazninu. To izgleda nerealno.

### Goal

Uvesti **strogo pravilo**: **svaki artwork slot dobije točno 1 spotlight** koji cilja **točan centar** tog artworka.

### MUST

1. **Disable/Remove legacy static spotlights**

* Ako u sceni postoji stari set spotova (hardcoded), privremeno ga ugasiti ili ukloniti.
* Ostaviti samo fill (ambient/hemisphere) dok se novi spotovi ne potvrde.

2. **Generate spotlights only from active artwork slots**

* `artworkSlots.filter(s => s.hasArtwork)`
* Za svaki slot generirati 1 spotlight.

3. **Each spotlight targets world-center of artwork**

* Target mora biti zasebni Object3D u scene graphu.
* U `useFrame` raditi `light.target.updateMatrixWorld()`.

4. **Cone discipline**

* Uži angle (gallery feel): npr. `Math.PI/10` do `Math.PI/8`
* Penumbra: 0.6–0.75
* Distance: dovoljno (12–18), bez pretjerivanja.

5. **Never-black guarantee**

* `ambientLight` ili `hemisphereLight` mora ostati uključen:

  * intensity 0.22–0.35

### Stop conditions

* [ ] Nijedan spotlight ne osvjetljava dvije slike
* [ ] Svaka slika ima smislen “own light”
* [ ] Nema crne sobe
* [ ] Screenshot Center + Side view

---

## 👩‍💻 Code helper: Strict generator (drop-in pattern)

> Agent: prilagodi imenima varijabli/slotova u projektu. Logika je bitna.

```tsx
// 1) Keep a minimal fill to prevent black-room regression
<ambientLight intensity={0.28} />

// 2) Legacy lights OFF (example)
// If you have something like <LegacySpotlights enabled />, set enabled={false}
// or remove them when strict mode is active.

// 3) Strict: 1 artwork = 1 spotlight
{artworkSlots
  .filter((s) => s.hasArtwork)
  .map((slot) => {
    const targetWorld = getWorldCenter(slot.artworkObject3D);

    const ceilingY = GALLERY_CEILING_Y; // e.g. 4.8–5.2 depending on ceiling
    const offsetFromWall = 0.35;

    // wallNormal should point *out* from wall into the room
    const n = slot.wallNormal.clone().normalize();

    const lightWorld = targetWorld
      .clone()
      .add(n.clone().multiplyScalar(offsetFromWall))
      .setY(ceilingY - 0.2);

    return (
      <AutoSpotlight
        key={slot.id}
        targetWorld={targetWorld}
        lightWorld={lightWorld}
        intensity={1.25}
        angle={Math.PI / 9}
        penumbra={0.7}
        distance={16}
        castShadow
      />
    );
  })}
```

### Debug tip (if lights aim wrong)

* Vizualiziraj wallNormal orijentaciju: ako je normal okrenuta prema zidu, offset ide "iza" zida → svjetlo neće raditi.
* U tom slučaju invertirati normalu: `n.multiplyScalar(-1)`.

---

## 🆕 TASK 5.2 — Lighting v2: Strict "1 Artwork = 1 Spotlight" (STABLE MODE)

### Goal

Postići **realističnu galerijsku rasvjetu** gdje **svaka slika ima svoju lampu**, bez generičkih cone mrlja i **bez ikakve mogućnosti crne sobe**.

Ovaj task se radi **isključivo na stabilnom snapshotu** (non-black room).

---

### RULE 0 — Never Black Room (OBAVEZNO)

Ovaj dio **mora uvijek ostati uključen**, bez iznimke.

```tsx
// Baseline fill — nikad ne gasiti
<ambientLight intensity={0.28} />
// ili (alternativa)
<hemisphereLight intensity={0.28} />
```

Ako se koristi oba:

```tsx
<ambientLight intensity={0.18} />
<hemisphereLight intensity={0.18} />
```

---

### STEP 1 — Ukloniti stare spotlightove

* Disable / remove **sve legacy spotlightove** (hardcoded po stropu ili zidu).
* Ne smije ostati dupla rasvjeta.

---

### STEP 2 — Strict pravilo: 1 artwork = 1 spotlight

* Generirati spotlightove **isključivo iz artwork slotova**.
* `artworkSlots.filter(s => s.hasArtwork)`
* Za svaki slot → **1 spotlight**.

---

### STEP 3 — Pozicija i target (track-light logika)

```tsx
{artworkSlots.filter(s => s.hasArtwork).map(slot => {
  const targetWorld = getWorldCenter(slot.artworkObject3D);

  const ceilingY = GALLERY_CEILING_Y;   // npr. 5.0
  const offsetFromWall = 0.35;          // 35 cm od zida

  // wallNormal mora gledati iz zida prema prostoriji
  const n = slot.wallNormal.clone().normalize();

  const lightWorld = targetWorld.clone()
    .add(n.multiplyScalar(offsetFromWall))
    .setY(ceilingY - 0.2);

  return (
    <AutoSpotlight
      key={slot.id}
      targetWorld={targetWorld}
      lightWorld={lightWorld}
      intensity={1.15}
      angle={Math.PI / 9}
      penumbra={0.72}
      distance={16}
      castShadow
    />
  );
})}
```

---

### STEP 4 — Stabilnost (bez trzanja)

Ako se slotovi/pozicije mogu mijenjati, koristiti **lerp/damping** u `useFrame`:

* `light.position.lerp(...)`
* `light.target.position.lerp(...)`
* obavezno `light.target.updateMatrixWorld()`

---

### STEP 5 — Debug zaštita (ako nešto ne svijetli kako treba)

* Ako spotlight ne pogađa zid → invert normalu:

```ts
n.multiplyScalar(-1)
```

* Ako je snop preuzak → privremeno:

```ts
angle={Math.PI / 7}
```

---

### Stop conditions (MUST)

* [ ] Nijedan spotlight ne osvjetljava dvije slike
* [ ] Svaka slika ima svoju jasno povezanu lampu
* [ ] Prostor nikad ne postaje crn
* [ ] Screenshot: Center + Back Left

---

### VAŽNO

Ako se **u bilo kojem trenutku pojavi crna soba**:

1. Odmah rollback na stabilni snapshot
2. Provjeriti da je RULE 0 (ambient/hemisphere) aktivan
3. Tek onda nastaviti
